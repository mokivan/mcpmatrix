# Resolver Summary

Canonical spec:

- `docs/specs/spec-resolver.md`

Summary:

`servers_final = global.enable + tags.enable + repo.enable`

Then:

- remove duplicates
- preserve first appearance order
